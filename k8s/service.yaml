apiVersion: v1
kind: Service
metadata:
  name: ecommerce-agent-service
  labels:
    app: ecommerce-agent
spec:
  selector:
    app: ecommerce-agent
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort